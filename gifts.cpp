#include<bits/stdc++.h>
#define maxn (int) 1e6 + 5
using namespace std;
int n;
int main() {
    cin >> n >> k;
    vector<int> a(n);
    for(int i = 0; i < n; i++) {
        cin >> a[i];
    }
    return 0;
}
