#include<bits/stdc++.h>

using namespace std;
int a[(int) 2e3 + 5];
int main() {
    int q;
    cin >> q;
    while(q--) {
        int n, k, m, r;
        cin >> n >> k >> m >> r;
        for(int i = 1; i <= n; i++) {
            cin >> a[i];
        }
        
    }
}